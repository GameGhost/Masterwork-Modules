## Bundled/release artifacts — these are built outputs, not source
*.zip
*.mwm

## Extractor-generated report — written next to whichever .source/*.cs was read, regenerated by
## every extraction run
_extraction-report.md
