# Purely decorative for this template — the progress footer shared by narration/introduction/
# hub_early/hub_middle/hub_late reads this to highlight "the current round" pip. Each demo passage
# just assigns whatever round label makes sense for its own screenshot; there's no real round-by-
# round game state being simulated here.
variables:
  roundLabel: 'string'
