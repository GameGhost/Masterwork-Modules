# Replaces the reference app's bespoke ViewBiddingSystem (voting/bidding) component — see
# README.md "Voting/bidding" for the full pattern. No chrome regions needed; the countdown
# animation and delayed Okay-button enable both live in style.css against
# .mws-popup-overlay.layout-reveal_countdown.
format: 'mws/0.4'
layout_id: 'reveal_countdown'
