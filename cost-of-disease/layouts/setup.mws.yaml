# Stub — no chrome authored yet. Popup layout: 'setup' (~158 popups — the item-obtain/round-setup
# notification popup; already has its own dedicated visual treatment via
# assets/style.css's .mws-popup-overlay.layout-setup rules and PopupNode.Header, so this file is
# only relevant if the layout also needs chrome outside the popup's own header/content/footer).
format: 'mws/0.4'
layout_id: 'setup'
header: []
footer: []
before_content: []
after_content: []
