# Stub — no chrome authored yet. Popup layout: 'countdown_instructions' (9 occurrences) — the outer
# half of the bidding/voting nested-popup pattern (replaces the old flat 'bidding'/'voting' layouts
# and the bespoke VotingPopupContent component, both retired). See
# Masterwork-Modules/my-fathers-work-template's own countdown_instructions.mws.yaml for the
# real, styled version this can be based on.
format: 'mws/0.4'
layout_id: 'countdown_instructions'
header: []
footer: []
before_content: []
after_content: []
