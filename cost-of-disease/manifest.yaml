id: 'renegade.cost_of_disease'
version: '0.5.0'
languages:
  - 'en-US'
title: 
  - en-US: 'The Cost of Disease'
description: 
  - en-US: 'The Yellow Fever had struck the village, and for several weeks any visit to its somber limits forced the young heirs to keep a cloth handkerchief about their mouth and nostrils to protect from the miasma. While they were remiss to admit it, this seemed the perfect macabre backdrop in which to advance their father''s dreadful business.'
thumbnail:
  image: 'image://scenario_tile_tcod'
info:
  players-min: 2
  players-max: 4
  playtime: 
  - en-US: '180 minutes'
entry: 'Setup_01_PlayerCountSelect'

# Extractor-owned passages vs. hand-maintained ones. Base passages load first; passage_overrides
# load after and win on a matching passage_id (new IDs are simply added). Re-running the extractor
# only touches passages/, so hand-authored content here always survives a re-extraction.
passages: 'passages'
passages_override: 'passages-override'

# This module's own visual styling — the app's chrome only emits structural class hooks
# (layout-{value}, style-{value}); everything those hooks actually look like lives here.
style: 'assets/style.css'

# Standalone module — no dependencies field/mechanism exists yet for a real shared asset pack
# (masterwork-plan Q27). For now, my-fathers-work-template's layouts/style.css/fonts/etc. are
# applied by direct copy (see README.md's Layout table) rather than a runtime dependency.
dependencies: []

# Module-wide default background music and SFX — see docs/mws-format-latest.md §9 (Audio) in the
# code repo. Real audio, sourced from Renegade Game Studios' own community-resources release for
# *My Father's Work* — see NOTICE.md's "Audio assets" section for full provenance. Applies to every
# passage/popup/link in this module that doesn't declare its own audio: override.
#
# music.default_tracks (Phase 5 Milestone 6.2): a shared 4-track sequence playlist, template-
# canonical (assets/audio/bgm/, propagated via scripts/apply-template.ps1) and identical across all
# three official modules — in the original app this same set (Chronicle Part One/Two/Three plus
# this module's own theme track) played together as ViewGenerationEnding's own end-of-generation
# playlist; used here as the general module-wide default instead, not scoped to any one popup type.
audio:
  music:
    default_tracks:
    - 'audio://bgm/cost_of_disease_theme'
    - 'audio://bgm/chronicle_part_1'
    - 'audio://bgm/chronicle_part_2'
    - 'audio://bgm/chronicle_part_3'
    order: 'sequence'
  sfx:
    popup_open:
    - 'audio://sfx/popup_open'
    popup_close:
    - 'audio://sfx/popup_close'
    click:
    - 'audio://sfx/click'

# TODO (Milestone C/D, not yet parsed by ManifestParser.cs): types, achievements, popups
# per docs/mws-format-latest.md §9/§8. Fill these in as each feature actually gets built — don't
# pre-write a schema this module can't yet load.
