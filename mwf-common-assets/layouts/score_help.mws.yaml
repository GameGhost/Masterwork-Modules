# No chrome regions needed — the score-entry help popup visual treatment (screenshot:
# Scoring-02-Score-Entry-Help-popup.png) lives entirely in style.css against
# .mws-popup-overlay.layout-score_help. Same popup_parchment_border.png background as score_panel's
# own inner panel, with a real button_close.png close button (top-right) instead of a text button.
format: 'mws/0.4'
layout_id: 'score_help'
