# No chrome regions needed — the "dark bordered box" visual treatment (screenshot: "Choose Audio
# Voice") lives entirely in style.css against .mws-popup-overlay.layout-choice. Empty chrome file
# kept for discoverability, matching the convention already established by layouts/setup.mws.yaml.
format: 'mws/0.4'
layout_id: 'choice'
