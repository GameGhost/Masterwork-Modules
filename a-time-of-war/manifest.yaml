id: 'renegade.a_time_of_war'
version: '0.1.0'
languages:
  - 'en-US'
title:
  - en-US: 'A Time of War'
description:
  - en-US: 'My father was a genius! I state this now not with the naivete of my initial discovery of his journal, but as a time-tested statement of truth. Weary as I was from the excursion across the Hungarian plains, nothing could have prepared me for the secret laboratory hidden beneath the manor -- or what it would ask of our family in the years to come.'
# Placeholder — no curated thumbnail art sourced yet for this module (see the ending overrides'
# own comment on the same gap for game_complete's header image). Reuses the already-resolving
# scenariobox3d_war icon in the meantime.
thumbnail:
  image: 'icon://scenariobox3d_war'
info:
  players-min: 2
  players-max: 5
  playtime:
  - en-US: '180 minutes'
entry: 'Setup_01_PlayerCountSelect'

# Extractor-owned passages vs. hand-maintained ones. Base passages load first; passage_overrides
# load after and win on a matching passage_id (new IDs are simply added). Re-running the extractor
# only touches passages/, so hand-authored content here always survives a re-extraction.
passages: 'passages'
passages_override: 'passages-override'

# This module's own visual styling — the app's chrome only emits structural class hooks
# (layout-{value}, style-{value}); everything those hooks actually look like lives here.
style: 'assets/style.css'

# Standalone module — no dependencies field/mechanism exists yet for a real shared asset pack
# (masterwork-plan Q27). For now, my-fathers-work-template's layouts/style.css/fonts/etc. are
# applied by direct copy (see scripts/apply-template.ps1) rather than a runtime dependency.
dependencies: []
